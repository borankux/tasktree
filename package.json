{
  "name": "tasktree",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "server": "pnpm --filter @tasktree/server dev",
    "web": "pnpm --filter @tasktree/web dev"
  },
  "onlyBuiltDependencies": [
    "better-sqlite3",
    "esbuild"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}